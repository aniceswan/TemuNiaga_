DATABASE_URL="postgresql://temuniaga@127.0.0.1:5434/temuniaga_dev"
JWT_SECRET="dev-only-secret-change-me"
PORT=3501

# BPS webapi.bps.go.id key (get one at https://webapi.bps.go.id/developer/).
# Used by POST /harga/refresh to populate the PriceCache -- domain/var dataset
# IDs are commodity/region-specific and passed per-request, not configured here.
BPS_API_KEY=
