{
  "name": "@temuniaga/shared-types",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "lint": "echo 'no lint configured for shared-types package'",
    "test": "echo 'no tests configured for shared-types package'"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
