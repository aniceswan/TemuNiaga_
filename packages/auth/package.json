{
  "name": "@temuniaga/auth",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "lint": "echo 'no lint configured for auth package'",
    "test": "echo 'no tests configured for auth package'"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^20.14.0"
  }
}
