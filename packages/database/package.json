{
  "name": "@temuniaga/database",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "seed": "tsx prisma/seed/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "prisma generate",
    "lint": "echo 'no lint configured for database package'",
    "test": "echo 'no tests configured for database package'"
  },
  "prisma": {
    "seed": "tsx prisma/seed/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0",
    "tsx": "^4.19.0",
    "csv-parse": "^5.6.0",
    "typescript": "^5.6.0",
    "@types/node": "^20.14.0"
  }
}
