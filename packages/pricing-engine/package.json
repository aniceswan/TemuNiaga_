{
  "name": "@temuniaga/pricing-engine",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "lint": "echo 'no lint configured for pricing-engine package'",
    "test": "echo 'no tests configured for pricing-engine package'"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
